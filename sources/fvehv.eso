C FVEHV     SOURCE    AM        11/05/26    21:15:34     6982
      subroutine FVEHV (force,q,XMKel,Gradfs,hh)
C     Routine permettant le calcul de la fonction vectorielle d'écrouissage pour le mécanisme vertical

        IMPLICIT INTEGER(I-N)
        IMPLICIT REAL*8(A-H,O-Z)
*
        DIMENSION XMKel(5,5), force(5), Gradfs(5)
*
        hh=-Gradfs(1)*XMKel(1,1)*(1-q)/q

        return
        end

